<div class="comments">
  {{this.id}}
  {{!-- {{post}} --}}
  {{!-- {{id}} --}}
  {{#each this as |comment|}} 
  <div class="comment">
  {{#each comment}}
        <div class="comment-author">
             <p>{{comment.id}}</p>
        </div>
        <div class="comment_body">
            <p>{{comment.comment_body}}</p>
            <p>lmao lol wtf </p>
        </div>
    {{/each}}
    </div>
  {{/each}} 
</div>
