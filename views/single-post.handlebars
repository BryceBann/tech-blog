{{>post-info post}}

    {{#if logged_in}}
        <form class="card-body comment-form" id="comment-form">
                <textarea id="comment" name="commentBody" class="form-input form-control" />
            <button type="submit" class="btn comment-btn">submit</button>
        </form>
    {{/if}}

    {{> comments post.comments}}


{{#if logged_in}}
<script src="/javascript/comment.js"></script>
{{/if}}